package com.jonLwiza.engine.actors
{
	import com.jonLwiza.engine.baseConstructs.SceneHandler;

	public class AllpurposeSceneHandler extends SceneHandler
	{
		// overrides the method that searches for the hero and searches for anything that comes in it 
		// its takes up alot of memory and stuff for this to be efficient would require a lot of code, and the benifit isnt too much
		// i probably wont ever make it, but its here incase I need it
		public function AllpurposeSceneHandler()
		{
			super();
		}
	}
}